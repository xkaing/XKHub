'use client'

import { Typography } from 'antd'

const { Title } = Typography

export default function GameIPsPage() {
  return (
    <div>
      <Title level={2}>GAME IP</Title>
      <p>游戏IP页面 - 功能开发中</p>
    </div>
  )
}